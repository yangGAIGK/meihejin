<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.platform.magnesium_alloy_platform.mapper.ImageMapper">
    <insert id="insertImage">
        INSERT INTO image (ImageUrl, BinaryUrl, createTime, uid)
        VALUES (#{imageMap.imageUrl}, #{imageMap.binaryUrl}, #{imageMap.createTime}, #{imageMap.uid})
    </insert>
    <delete id="deleteImagesByImageUrls">
        DELETE FROM image
        WHERE ImageUrl IN
        <foreach item="item" index="index" collection="imageUrls" open="(" separator="," close=")">
            #{item}
        </foreach>
    </delete>
    <select id="queryImages" resultType="java.util.Map">
        SELECT ImageUrl, BinaryUrl, createTime
        FROM image
        LIMIT #{offset}, #{pageSize}
    </select>
    <select id="countImages" resultType="long">
        SELECT COUNT(*) FROM image
    </select>
</mapper>